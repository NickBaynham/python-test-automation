# To Do

All planned phases (0 through 3) are complete: the platform tests React UIs
across the host's available browsers, REST APIs, and MongoDB state, with a
full-stack scenario spanning all three layers.

No features are currently under construction. Candidate future work, not yet
planned or committed:

- Headed browser mode for local debugging (a `TP_HEADLESS=false` setting).
- HTML/Allure report publishing to a hosted location from CI.
- Additional target application adapters as real systems under test arrive.
